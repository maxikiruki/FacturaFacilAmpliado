{% extends 'base.html.twig' %}

{% block title %}Editar red
{% endblock %}

{% block body %}
	<h1>Editar red</h1>

	<div class="row">
		<div class="col-md-5">
			{{ include('network/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('network_index', {'identerprise': identerprise}) }}">Regresar a redes sociales</a>

			{{ include('network/_delete_form.html.twig') }}

		</div>
	</div>


{% endblock %}
