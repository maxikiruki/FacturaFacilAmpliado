{% extends 'base.html.twig' %}

{% block title %}Editar Responsable
{% endblock %}

{% block nav %}
	{{ include('navbar/shownavbar.html.twig') }}
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row">
			<div class="col-md-9">
				<h1>Editar Responsable</h1>

				{{ include('supervisor/_form.html.twig', {'button_label': 'Update'}) }}

				{# <a href="{{ path('supervisor_index') }}">Regreso a indice de supervisores</a> #}

				{{ include('supervisor/_delete_form.html.twig') }}


			</div>
		</div>
	</div>
{% endblock %}
