{% extends 'base.html.twig' %}

{% block title %}Red social
{% endblock %}

{% block body %}
	<h1>Red social</h1>
	<div class="container" id="tableArea">

		<table class="table table-striped">
			<tbody>
				<tr>
					<th>Url</th>
					<td>{{ social_network.url }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('social_network_index') }}">Regresar a listado de red social</a>

		<a href="{{ path('social_network_edit', {'id': social_network.id}) }}">Editar</a>

		{{ include('social_network/_delete_form.html.twig') }}
	</div>
{% endblock %}
