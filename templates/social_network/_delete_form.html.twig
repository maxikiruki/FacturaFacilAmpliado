<form method="post" action="{{ path('social_network_delete', {'id': social_network.id, 'identerprise': identerprise}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ social_network.id) }}">
    <button class="btn">Borrar</button>
</form>
