{% extends 'base.html.twig' %}

{% block title %}Editar red social
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row">
			<div class="col-md-9">
				<h1>Editar red social</h1>

				{{ include('social_network/_form.html.twig', {'button_label': 'Update'}) }}

				{# <a href="{{ path('social_network_index', {identerprise: identerprise}) }}">Regresar a lista de redes sociales</a> #}

				{{ include('social_network/_delete_form.html.twig') }}

			</div>
		</div>
	</div>
{% endblock %}
